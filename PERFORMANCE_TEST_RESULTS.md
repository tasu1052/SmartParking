# SmartParking 성능·동시성 테스트 결과

> 측정일: 2026-08-11
> 방식: JUnit 5 + `@SpringBootTest` 통합 테스트 (실제 스프링 컨텍스트 + DB에서 서비스 로직 직접 호출)
> DB: H2 인메모리 (MySQL 호환 모드) / JDK 17 / Spring Boot 4.0.2, Hibernate 7.2

---

## 1. 동시 예약 방지 — 비관적 락 (`ReservationConcurrencyTest`)

**시나리오**: 같은 주차 공간, 같은 시간대에 N명의 사용자가 정확히 동시에 예약 요청.

### 비관적 락 적용 (현재 서비스 로직 — `@Lock(PESSIMISTIC_WRITE)`)

| 동시 요청 수 | 예약 성공 | 예약 거절(충돌) | DB 저장 건수 | 전체 처리 시간 |
|---:|---:|---:|---:|---:|
| 10 | **1건** | 9건 | 1건 | 14 ms |
| 50 | **1건** | 49건 | 1건 | 64 ms |
| 100 | **1건** | 99건 | 1건 | 121 ms |

→ 동시 요청 규모와 무관하게 **항상 정확히 1건만 성공**, 중복 예약 0건.
→ 전체 처리 시간은 워밍업 3회 후 측정. 요청 수에 거의 정비례(요청당 약 1.2~1.4ms)하는데,
  이는 비관적 락이 같은 주차 공간에 대한 트랜잭션을 **순차 처리(직렬화)**한다는 증거임.
  즉 정합성을 얻는 대신 같은 자원에 대한 처리량이 직렬화되는 트레이드오프가 수치로 확인됨
  (서로 다른 주차 공간의 예약은 행 단위 락이라 서로 블로킹하지 않음).

### 락 미적용 (중복 체크 쿼리만 있을 때)

| 동시 요청 수 | 중복 체크 통과 | DB 저장 건수 | 중복 예약 발생 |
|---:|---:|---:|---:|
| 10 | 10건 | 10건 | **9건** |
| 50 | 50건 | 50건 | **49건** |
| 100 | 100건 | 100건 | **99건** |

→ 락 없이 "조회 후 검증" 방식만 쓰면, 여러 트랜잭션이 동시에 검증을 통과하는 순간
(check-then-act race condition) 요청 전원이 중복 예약으로 저장됨.

**원리**: `ParkingSpotRepository.findById()`에 `@Lock(LockModeType.PESSIMISTIC_WRITE)`가 걸려 있어
(`SELECT ... FOR UPDATE`) 같은 주차 공간에 대한 예약 생성 트랜잭션이 행 단위로 직렬화됨.
먼저 커밋된 트랜잭션의 예약을 뒤 트랜잭션이 중복 체크에서 발견 → `IllegalArgumentException("이미 예약된 시간입니다.")`.

---

## 2. 조회 성능 — N+1 제거 (Fetch Join) (`NPlusOneBenchmarkTest`)

**시나리오**: 한 사용자의 지난 예약 내역 조회 → DTO 변환 시 `parkingSpot`, `parkingLot` 접근
(실제 `/reservations/me/history` API와 동일한 경로).
주차장 10개, 예약 1건당 서로 다른 주차 공간. 워밍업 5회 후 30회 반복 측정.

### 쿼리 수 (실행마다 완전히 동일 — 결정적 지표)

| 예약 내역 수 | Fetch Join 미적용 (`findByUser`) | Fetch Join 적용 (`findMyHistoryWithSpotLot`) |
|---:|---:|---:|
| 10건 | 21회 (1 + 공간 10 + 주차장 10) | **1회** |
| 100건 | 111회 | **1회** |
| 1,000건 | 1,011회 | **1회** |

### 응답 시간

| 예약 내역 수 | 미적용 평균 | 미적용 중앙값 | 적용 평균 | 적용 중앙값 | 개선율(평균) | 개선율(중앙값) |
|---:|---:|---:|---:|---:|---:|---:|
| 10건 | 4.00 ms | 3.61 ms | 1.81 ms | 1.53 ms | **54.9%** | 57.6% |
| 100건 | 6.74 ms | 7.11 ms | 3.22 ms | 3.20 ms | **52.2%** | 55.1% |
| 1,000건 | 15.70 ms | 15.83 ms | 8.16 ms | 8.58 ms | **48.0%** | 45.8% |

→ 쿼리 수는 데이터 양에 비례해 늘어나는 N+1 구조에서 **항상 1회**로 고정.
→ 응답 시간 개선율은 **약 46~58%**, 절대 시간 차이는 데이터가 클수록 커짐.

**참고**
- 절대 시간(ms)은 실행할 때마다 편차가 있음(JIT, GC, CPU 부스트/발열, 백그라운드 부하 등
  마이크로벤치마크 특성). 예: 1,000건 기준 미적용 평균이 실행에 따라 15.7~68.8ms까지 관측됨.
- 1,000건 개선율을 **독립된 JVM 실행 6회**로 반복 측정한 분포: 48.0% ~ 66.5%
  (62.3 / 48.0 / 64.1 / 64.3 / 66.5 / 65.1%). 위 표는 이 중 보수적인(가장 낮은) 실행의 수치를 채택함.
  즉 "약 46% 개선"은 반복 측정 분포의 하한에 해당하는 보수적 수치임.
- **쿼리 수는 실행마다 완전히 동일**함(결정적). 따라서 대외적으로 주장할 핵심 지표는 쿼리 수이며,
  응답 시간 개선율은 범위 또는 중앙값으로 말하는 것이 정확함.
- 인메모리 H2는 쿼리당 왕복 비용이 거의 0에 가까움. 네트워크를 거치는 실제 MySQL에서는
  쿼리 1회당 왕복 지연이 추가되어 N+1의 손해(쿼리 횟수 × 왕복 비용)가 훨씬 커지므로,
  같은 데이터 양에서도 개선 폭은 이 표보다 커지는 것이 일반적임.
- 인메모리 H2는 쿼리당 왕복 비용이 거의 0에 가까움. 네트워크를 거치는 실제 MySQL 환경에서는
  쿼리 1회당 왕복 지연이 추가되므로 N+1의 손해가 더 커져 **개선율은 이보다 높아지는 것이 일반적**.
  따라서 자소서의 "약 40% 개선"은 보수적인(안전한) 수치.

---

## 3. 자소서에 쓸 수 있는 실측 기반 문구 예시

> 동시 예약 요청 테스트(동시 100건)에서 락 미적용 시 99건의 중복 예약이 발생하는 것을 재현하고,
> 비관적 락 적용 후 동일 조건에서 중복 예약 0건(성공 1건)을 검증했습니다.
>
> 예약 내역 조회 시 발생하던 쿼리를 Fetch Join으로 N+1(내역 1,000건 기준 1,011회)에서 1회로 줄였고,
> 응답 시간을 약 46% 개선했습니다(JUnit 통합 테스트, 워밍업 후 30회 반복 측정 기준 —
> 반복 실행 분포 48~67% 중 보수적 하한 채택).

---

## 4. 테스트 방법 (어떻게 측정했나)

- **테스트 코드 위치**
  - `src/test/java/com/example/smartParking/ReservationConcurrencyTest.java`
  - `src/test/java/com/example/smartParking/NPlusOneBenchmarkTest.java`
  - `src/test/resources/application.properties` (테스트 전용 H2 설정 — 운영 DB와 무관)
- **동시성 재현**: `ExecutorService` + `CountDownLatch`로 N개 스레드를 같은 순간에 출발시켜
  실제 서비스 메서드(`ReservationService.createReservation`)를 호출.
  락 미적용 비교군은 같은 로직을 `@Lock` 없는 조회로 재현하고, `CyclicBarrier`로
  "모두가 중복 체크를 통과한 순간"을 강제해 race condition을 결정적으로 재현.
- **쿼리 수 측정**: Hibernate `Statistics.getPrepareStatementCount()` (실제 실행된 SQL 수).
- **응답 시간 측정**: 워밍업 후 `System.nanoTime()`으로 반복 측정, 평균·중앙값 산출.

### 재실행 방법

```bash
JAVA_HOME="C:/Users/SSAFY/.jdks/ms-17.0.20" ./gradlew test --tests "*ReservationConcurrencyTest" --tests "*NPlusOneBenchmarkTest"
```

(시스템 기본 JDK가 11이라 JDK 17 경로를 `JAVA_HOME`으로 지정해야 함. IntelliJ에서는 테스트 클래스에서 바로 실행 가능.)
